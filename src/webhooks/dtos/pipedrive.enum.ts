export enum PipedriveEntity {
  PERSON = 'person',
  ORGANIZATION = 'organization',
  DEAL = 'deal',
  PIPELINE = 'pipeline',
  STAGE = 'stage',
}

export enum PipedriveAction {
  CREATE = 'create',
  CHANGE = 'change',
  DELETE = 'delete',
}
