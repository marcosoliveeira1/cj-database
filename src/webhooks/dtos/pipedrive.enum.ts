export enum PipedriveEntity {
  PERSON = 'person',
  ORGANIZATION = 'organization',
  DEAL = 'deal',
  PIPELINE = 'pipeline',
  STAGE = 'stage',
  USER = 'user',
}

export enum PipedriveAction {
  CREATE = 'create',
  CHANGE = 'change',
  DELETE = 'delete',
}
